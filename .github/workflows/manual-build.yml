name: Manual Build

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version to build (commit SHA, tag, or branch name)"
        required: true
        type: string
        default: "main"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.version }}

      - uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Unit tests
        run: go test ./... -count=1

      - name: Build linux amd64
        run: |
          GOOS=linux GOARCH=amd64 go build -o query-session-linux-amd64 ./cmd/query-session

      - name: Build darwin arm64
        run: |
          GOOS=darwin GOARCH=arm64 go build -o query-session-macos-arm64 ./cmd/query-session

      - name: Upload linux amd64
        uses: actions/upload-artifact@v4
        with:
          name: query-session-linux-amd64-${{ inputs.version }}
          path: query-session-linux-amd64

      - name: Upload macos arm64
        uses: actions/upload-artifact@v4
        with:
          name: query-session-macos-arm64-${{ inputs.version }}
          path: query-session-macos-arm64
